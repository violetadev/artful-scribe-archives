[build]
  command = "npm run build"
  publish = "dist/client"

[build.environment]
  NODE_VERSION = "22"

# SPA fallback for client-side routes (Netlify serves static files first)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
