/** Update these fields when you publish a new build. */
export const studioRelease = {
  version: "0.1.0",
  /** ISO date (YYYY-MM-DD) */
  releasedAt: "2026-05-21",
  /** Direct link to the installer (GitHub Releases, etc.) */
  downloadUrl: "",
  platform: "Windows",
} as const;
